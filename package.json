{
  "name": "vssh-app-toolkit",
  "version": "2.0.0",
  "//version": "Major bumpado porque as libs de lib/ carregam breaking changes reais desde a tag v1 — permissão que parou de mentir 'granted', getFile() preguiçoso, timeout default de 0 para 600s, entre outros. Estava congelada em 1.0.0 desde o commit inicial, o que fazia o único marcador de procedência do consumidor (vendor/vssh/.vssh-lib-version) apontar para um número que não distinguia nada. Ver MIGRATION.md.",
  "private": true,
  "description": "Ferramentas e bibliotecas para construir e publicar vssh-apps",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "//engines": "O >=22 é do RUNNER DE TESTE (o glob abaixo só existe a partir dessa versão). As libs de lib/ rodam em Node >=20 — o CI prova isso rodando o template em 20, e é essa a versão que importa, porque é a do servidor-alvo.",
    "//test": "Glob entre aspas: quem expande é o Node, não o shell — sem isso quebra no cmd.exe do Windows, que é metade da matriz do CI.",
    "test": "node --test \"lib/**/*.test.js\""
  }
}
